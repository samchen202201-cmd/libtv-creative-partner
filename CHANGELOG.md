# Changelog

## v0.18.1 — 2026-09-11

> 只修可复用模板里的文字缺陷，不增加新方法。来源：Reasonix 对 v0.18.0 的评审（R1、R5、R6）。

- 资产模块三联定妆模板：左栏去掉指代不清的「这张」，正面发型、发际线和耳部结构明确由左侧特写承担；中间栏写明与右侧同比例绘制、上方留白、不放大人物填满栏高，避免和电商版型图的取景互相拉扯；「肤色」改为只写剧本或已选参考中明确的，未明确时给出建议并标明。

## v0.18.0 — 2026-09-11

> 来源：《一毫米的战场》跑片实测、一份写实人像 Prompt 的拆解，以及 Fable 与 Codex 两份 v0.17.0 评审。
> 未采纳 Codex 将三联模板改回面部遮挡块的建议：无头正面全身是 2026-09-10 的版式决定；只吸收其“减少负面语义”的意见。

- 分镜模块增加生成分组判据：机位不变、动作连续的镜头优先合成一条生成；新增显式锚点 `shot-continuity`，修复原先失效的内部链接。
- Prompt 模块增加单条生成的节拍上限（按目标模型实测，不写死常数）与字段取舍顺序；结束状态重定义为切出时的动作阶段与动势，控制漂移时给量、不下禁令，以终点构图作验收。
- 完整指南：入点由内容锁定；已交付指南局部修改时同步改文件本身；HTML 只在明确要求网页时触发。
- 资产模块：三联模板中间一栏改为正向取景描述；写实真人质感分为基础与按角色选用两层，修正镜头参数“决定透视”的表述；新增成组出图的身份锚点与角度分配、本色验收锚点、陈设道具的视觉权重、造型阶段避开高频崩坏结构；拆分身份互换段落。
- 入口：`working-principles.md` 并入 SKILL.md 后删除；触发描述补充“负责创作准备与返修，不默认执行生成”的边界；拆分共同创作方式首条。
- 发布：增加 `.gitignore`。

## v0.17.0 — 2026-09-10

- 资产模块增加“写实真人质感”小节：把写实需求落到可见毛孔、肤色纹理、虹膜与睫毛等可核对的画面事实，列出无效的获奖级后缀，并区分服务于近景关键帧的镜头与布光规格。
- 角色定妆三联模板改为“无头正面全身”：中间那张头部不入画，取代原先的肤色遮挡块，正面不再出现第二张脸；正面发型、发际线与耳部改由左侧特写承担，并在模板中写明该要求。
- 同步修正完整制作指南（complete-word-guide.md）里对三联模板的转述，避免和资产模块新表述不一致。

## v0.16.0 — 2026-09-09

> 合并两条并行分支：v0.14.3 的 Markdown 默认交付，与 v0.15.0 的四条内容规则。
> v0.15.0 曾被 v0.14.3 的改写覆盖，其内容在此重新并入。

- 资产模块找回“材质与使用痕迹”，把质感感受落到可见表面与来历清楚的痕迹。
- 角色资产找回形态与一致性成本的取舍说明，不更改已定角色身份。
- 故事提案找回主体跨镜可复现程度作为内部选择依据；并明确该判断不进入交给用户的文字，可复现程度、拍摄成本、后期与生成难度都不作为推荐理由写进提案正文。
- 完整指南找回实际生成量的余量提示，并允许指出本片哪几镜最费。
- 完整指南增加单页 HTML 交付选项：自包含、可打印，视觉方案由作品本身决定，不套通用模板。

## v0.14.3 — 2026-09-09

- 完整制作指南默认以 Markdown 文件交付；仅在用户明确要求 Word／`.docx` 或提供 Word 模板时调用 Word 生成器。
- 统一调整 Skill、README 与参考模块中的交付格式说明，避免把内置 Word 模板误当成默认要求。

## v0.14.2 — 2026-09-08

- 增加 MIT `LICENSE`，并补充开源使用说明。
- README 首屏改为产品介绍，版本变更移入本文件。
- 增加 Claude Code / Claude Desktop 的安装路径说明。
- 精简 Skill description，扩大剧本、分镜和视频 Prompt 的触发覆盖。
- 增加 `requirements.txt`，明确 Word 生成器的 `python-docx` 依赖。
- 增加跨平台中文字体回退说明。
- 增加 `column_widths()` 的宽度分配回归测试。

## v0.14.1 — 2026-09-07

- Word 生成器兼容普通代码围栏。
- 同名输出自动递增并保留旧稿。
- 集中共用规则，增加文档应急交付、按实际模型能力适配 Prompt，以及交付后的三行速览。
